# linux-system-snapshot
a basic cli tool built for linux that prints things such as: hostname, uptime, cpu usage data, ram usage data, disk usage data, logged-in users, and current data/time and saves collected data to a log file.
